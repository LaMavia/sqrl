* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html, body {
  width: 100vw;
  height: 100vh;
  overflow-x: hidden;
}

.app {
  width: 100vw;
  height: 100vh;
  display: flex;
  flex-flow: row nowrap;
  overflow-x: hidden;
  overflow-y: auto;
}